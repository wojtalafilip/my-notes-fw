blueBtn.addEventListener(`click`, function () {
  inputNote.className = ``;
  input.className = ``;
  inputNote.classList.add(`note`, `note--input`, `note--color-1`);
  input.classList.add(`input`, `color-1`);
  stage.color = 1;
});
greenBtn.addEventListener(`click`, function () {
  inputNote.className = ``;
  input.className = ``;
  inputNote.classList.add(`note`, `note--input`, `note--color-2`);
  input.classList.add(`input`, `color-2`);
  stage.color = 2;
});
yellowBtn.addEventListener(`click`, function () {
  inputNote.className = ``;
  input.className = ``;
  inputNote.classList.add(`note`, `note--input`, `note--color-3`);
  input.classList.add(`input`, `color-3`);
  stage.color = 3;
});
redBtn.addEventListener(`click`, function () {
  inputNote.className = ``;
  input.className = ``;
  inputNote.classList.add(`note`, `note--input`, `note--color-4`);
  input.classList.add(`input`, `color-4`);
  stage.color = 4;
});
pinkBtn.addEventListener(`click`, function () {
  inputNote.className = ``;
  input.className = ``;
  inputNote.classList.add(`note`, `note--input`, `note--color-5`);
  input.classList.add(`input`, `color-5`);
  stage.color = 5;
});
